{
  "name": "piquify",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "check-types": "turbo check-types",
    "typecheck": "turbo check-types",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.5.6",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@11.2.0"
}
